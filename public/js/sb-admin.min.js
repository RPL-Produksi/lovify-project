/*!
 * SB Admin 2 v4.1.1
 * Copyright 2024 Alejandro RH
 * Main Coded by Start Bootstrap (https://startbootstrap.com/)
 */

!(function (s) {
    "use strict";
    
    function handleLoadingButtons() {
        const forms = document.querySelectorAll(".form-with-loading");

        forms.forEach((form) => {
            const submitButton = form.querySelector(".btn-loading");
            const btnText = submitButton.querySelector(".btn-text");
            const spinner = submitButton.querySelector(".spinner-border");

            form.addEventListener("submit", () => {
                btnText.classList.add("d-none");
                spinner.classList.remove("d-none");
                submitButton.disabled = true;
            });
        });
    }

    function e() {
        s(window).width() < 768 && s(".sidebar .collapse").collapse("hide");
    }

    function o() {
        s(window).width() < 480 &&
            !s(".sidebar").hasClass("toggled") &&
            (s("body").addClass("sidebar-toggled"),
            s(".sidebar").addClass("toggled"),
            s(".sidebar .collapse").collapse("hide"));
    }

    // Sidebar toggle
    s("#sidebarToggle, #sidebarToggleTop").on("click", function (e) {
        e.preventDefault(),
            s("body").toggleClass("sidebar-toggled"),
            s(".sidebar").toggleClass("toggled"),
            s(".sidebar").hasClass("toggled") &&
                s(".sidebar .collapse").collapse("hide");
    }),
        e(),
        o(),
        s(window).resize(function () {
            e(), o();
        }),
        s("body.fixed-nav .sidebar").on(
            "mousewheel DOMMouseScroll wheel",
            function (e) {
                if (768 < s(window).width()) {
                    var o = e.originalEvent,
                        l = o.wheelDelta || -o.detail;
                    (this.scrollTop += 30 * (l < 0 ? 1 : -1)),
                        e.preventDefault();
                }
            }
        ),
        s(document).on("scroll", function () {
            100 < s(this).scrollTop()
                ? s(".scroll-to-top").fadeIn()
                : s(".scroll-to-top").fadeOut();
        }),
        s(document).on("click", "a.scroll-to-top", function (e) {
            var o = s(this);
            s("html, body")
                .stop()
                .animate(
                    { scrollTop: s(o.attr("href")).offset().top },
                    1e3,
                    "easeInOutExpo"
                ),
                e.preventDefault();
        });

    document.addEventListener("DOMContentLoaded", handleLoadingButtons);
})(jQuery);
